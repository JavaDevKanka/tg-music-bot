pluginManagement {
    resolutionStrategy {
        repositories {
            mavenCentral()
        }
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}
System.setProperty("file.encoding", "utf-8")

rootProject.name = "tg-music-bot"
